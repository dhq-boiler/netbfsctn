<Window x:Class="Netbfsctn.Tests.SampleWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sample WPF App" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox x:Name="InputBox" Grid.Row="0" Margin="10"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Calculate" Style="{StaticResource ButtonStyle}"
                    Click="OnCalculateClick" />
            <Button Content="Transform" Style="{StaticResource ButtonStyle}"
                    Click="OnTransformClick" />
        </StackPanel>

        <TextBlock x:Name="ResultBlock" Grid.Row="2" Margin="10"
                   Text="{Binding ResultText}" TextWrapping="Wrap" />
    </Grid>
</Window>
